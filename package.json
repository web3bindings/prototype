{
  "name": "@web3bindings/ethny-prototype",
  "version": "0.0.0-alpha",
  "engines": {
    "node": ">=9.4.0"
  },
  "scripts": {
    "postinstall": "npx lerna bootstrap",
    "start": "npm run start:protocol && npm run start:app",
    "start:app": "",
    "start:protocol": "cd ./protocol-example && npm run start && cd ../",
    "build:protocol": "cd ./protocol-example && npm run build && cd ../"
  },
  "devDependencies": {
    "lerna": "^3.14.1"
  }
}

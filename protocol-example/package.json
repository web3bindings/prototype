{
  "name": "@web3bindings/protocol-example",
  "version": "0.0.0-alpha",
  "engines": {
    "node": ">=9.4.0"
  },
  "scripts": {
    "start:env": "TODO start docker",
    "build": "TODO build protocol, migrate contracts, read, write",
    "deploy:subgraph": "deploy subgraph"
  }
}
